<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3>Editar Nota</h3>
            <div class="card-body">
                <form action="/notes/edit-note/{{note._id}}?_method=PUT" method="POST"> {{!----}}
                <input type="hidden"name ="_method" value="PUT"> {{!--par que el serv sepa que sale metodo put--}}
                    <div class="form-group">
                        <input type="text" name="titulo" class="form-control" placeholder="titulo" value="{{note.titulo}}">      
                                   {{!-- el servidor me est envindo un objeto llamado note, de ese solo quiero el titulo--}}         
                    </div>
                    <div class="form-group">
                        <textarea name="descripcion" class="form-control">{{note.descripcion}}</textarea>
                    </div>
                    <div class="form-group">
                        <button class="btn btn-primary btn-block" type="submit">
                            Guardar
                        </button>
                    </div>
                </form>
            </div>

        </div>
    </div>   
</div>